# Rentas24 Documentation Bundle

Put this folder in your frontend as:

`/documentation`

Then instruct your VS Code Agent:
- “Use /documentation as the only source of truth for Appwrite schema and rules.”
